claude
```

**4. כשהוא נפתח — הדבק את הפרומפט הבא בשלמותו**

---

## הפרומפט המלא
```
I need you to completely refactor and upgrade an existing kosher recipe web app 
called "מה יש בבית?". 

The existing codebase is currently a single index.html + netlify/functions/recipe.js.
You are working inside the project directory right now.

Infrastructure already in place (DO NOT change these):
- GitHub repo: esterlusti/mayesh-recipes  
- Netlify: incomparable-tiramisu-43b0b7.netlify.app
- Firebase project "mayeshrecipes":
    apiKey: "AIzaSyABfmxXuZlzHIXuGx0cHKxAdctQ_ep-_sA"
    authDomain: "mayeshrecipes.firebaseapp.com"
    projectId: "mayeshrecipes"
    storageBucket: "mayeshrecipes.firebasestorage.app"
    messagingSenderId: "261954124"
    appId: "1:261954124:web:decb95f2d5b2b2e72cb2e9"
- OpenAI API (gpt-4o) via Netlify Functions
- OPENAI_API_KEY is set in Netlify environment variables

---

## PART 1 — PROJECT STRUCTURE

Refactor into a proper React + Vite app with this exact structure:
```
recipe-app/
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── firebase.js
│   ├── components/
│   │   ├── AuthBar.jsx
│   │   ├── AuthModal.jsx
│   │   ├── GenderSelect.jsx
│   │   ├── ProfilePanel.jsx
│   │   ├── ProgressBar.jsx
│   │   └── RecipeResult.jsx
│   ├── steps/
│   │   ├── Step1Kosher.jsx
│   │   ├── Step2Equipment.jsx
│   │   ├── Step3Category.jsx
│   │   ├── Step4DishType.jsx
│   │   ├── Step5Ingredients.jsx
│   │   └── Step6Recipe.jsx
│   ├── data/
│   │   ├── categories.js
│   │   ├── ingredients.js
│   │   ├── equipment.js
│   │   └── sauces.js
│   ├── hooks/
│   │   ├── useAuth.js
│   │   ├── useGender.js
│   │   └── useUserData.js
│   └── styles/
│       └── index.css
├── netlify/
│   └── functions/
│       ├── recipe.js
│       └── server-local.js
├── public/
│   └── favicon.svg
├── index.html
├── vite.config.js
├── package.json
├── netlify.toml
├── .env.example
└── .gitignore
```

---

## PART 2 — LOCAL DEVELOPMENT (CRITICAL — MUST WORK)

The app must run fully locally with `npm run dev`.

### vite.config.js
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:9999',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  }
})
```

### netlify/functions/server-local.js
Create a standalone local server using only Node built-ins + dotenv:
```js
require('dotenv').config({ path: '../../.env' });
const http = require('http');
const { handler } = require('./recipe.js');

const PORT = 9999;

http.createServer(async (req, res) => {
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
  
  if (req.method === 'OPTIONS') {
    res.writeHead(204); res.end(); return;
  }

  if (req.method === 'POST' && req.url === '/recipe') {
    let body = '';
    req.on('data', chunk => body += chunk.toString());
    req.on('end', async () => {
      try {
        const result = await handler(
          { httpMethod: 'POST', body },
          {}
        );
        res.writeHead(result.statusCode, { 'Content-Type': 'application/json' });
        res.end(result.body);
      } catch (e) {
        res.writeHead(500);
        res.end(JSON.stringify({ error: e.message }));
      }
    });
  } else {
    res.writeHead(404); res.end();
  }
}).listen(PORT, () => {
  console.log(`✓ Local function server running on http://localhost:${PORT}`);
});
```

### package.json scripts:
```json
{
  "name": "mayesh-recipes",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"node netlify/functions/server-local.js\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.2.0",
    "concurrently": "^8.2.0",
    "dotenv": "^16.4.0"
  }
}
```

### .env.example:
```
OPENAI_API_KEY=sk-...
```

### .gitignore:
```
node_modules/
dist/
.env
.env.local
```

After creating all files, run `npm install` automatically.
Verify that `npm run dev` starts successfully before proceeding.

---

## PART 3 — FAVICON + BROWSER TAB

Create public/favicon.svg as a simple cooking pot SVG in terracotta color (#d4826a).

In index.html:
```html
<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>מה יש בבית? 🍳</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;0,900;1,700&family=Assistant:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.jsx"></script>
</body>
</html>
```

---

## PART 4 — DESIGN SYSTEM

### CSS Variables (src/styles/index.css):
```css
:root {
  --blush:     #f9f0eb;
  --petal:     #fdf6f0;
  --rose:      #d4826a;
  --rose-dk:   #b5614a;
  --rose-lt:   #e8a990;
  --rose-gl:   rgba(212,130,106,0.15);
  --mauve:     #c4879d;
  --sage:      #7a9e7e;
  --sage-lt:   rgba(122,158,126,0.12);
  --gold:      #c9a96e;
  --gold-lt:   rgba(201,169,110,0.15);
  --ink:       #2c1f18;
  --ink-60:    rgba(44,31,24,0.6);
  --ink-30:    rgba(44,31,24,0.3);
  --ink-10:    rgba(44,31,24,0.08);
  --milk:      #4a8ab5;
  --milk-lt:   rgba(74,138,181,0.12);
  --meat:      #9b3a48;
  --meat-lt:   rgba(155,58,72,0.12);
  --pareve:    #5a7a4e;
  --pareve-lt: rgba(90,122,78,0.12);
  --border:    rgba(44,31,24,0.09);
  --r:         20px;
  --r-sm:      13px;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  font-family: 'Assistant', sans-serif;
  background: var(--blush);
  color: var(--ink);
  direction: rtl;
  min-height: 100vh;
}
```

Aesthetic: feminine but not childish. Soft blush/rose palette, Playfair Display for 
all headings, rounded elements, subtle shadows. 
Decorative floating blobs (position:fixed, blur:90px, pointer-events:none, z-index:0).

---

## PART 5 — GENDER SELECTION (elegant, kitchen-themed)

### hooks/useGender.js
Manages gender state. Check Firestore (logged-in) or localStorage (guest).
Returns: { gender, setGender, genderLoaded }

### components/GenderSelect.jsx
Show this screen once, after login/guest choice, before the main app.
Skip if gender already saved.

Design: centered card over a blurred overlay.
```
Title (Playfair Display, 28px): "רגע לפני שמתחילים..."
Subtitle: "מה התפקיד שלך במטבח?"

Two large cards side by side:
┌──────────────────┐  ┌──────────────────┐
│                  │  │                  │
│      👨‍🍳           │  │      👩‍🍳           │
│    (64px emoji)  │  │    (64px emoji)  │
│                  │  │                  │
│      שף          │  │      שפית        │
│  (Playfair 22px) │  │  (Playfair 22px) │
│                  │  │                  │
└──────────────────┘  └──────────────────┘
```

On hover: card lifts + border appears in --rose color.
On click: save to Firestore users/{uid}/gender OR localStorage 'mayesh_gender', 
then proceed to app.

### Gender-aware text throughout the app:

Create a helper `useGenderText(male, female)` that returns the right string.

Examples:
| Location | Male | Female |
|---|---|---|
| AuthBar greeting | שלום, שף [name] 👨‍🍳 | שלום, שפית [name] 👩‍🍳 |
| Auth modal title | ברוך הבא! | ברוכה הבאה! |
| Step buttons | המשך | המשיכי |
| Generate button | צור לי מתכון ✨ | צרי לי מתכון ✨ |
| Loading text | מכין לך מתכון... | מכינה לך מתכון... |
| Recipe saved | המתכון נשמר! | המתכון נשמר! |
| Restart button | התחל מחדש | התחילי מחדש |
| Profile panel | הציוד שלי | הציוד שלי |

---

## PART 6 — HERO SECTION
```jsx
<section className="hero">
  <div className="hero-deco">✡️ כשר &nbsp;•&nbsp; בישול ביתי</div>
  <h1>מה יש לך<br/><em>בבית?</em></h1>
  <p className="hero-sub">ספרו לנו מה יש לכם במקרר ובמזווה —<br/>נמציא לכם מתכון כשר שאפשר להכין עכשיו</p>
  <p className="hero-joke">*חוץ מאסוך שמן 🫙</p>
</section>
```

hero-joke style: Playfair Display italic, font-size:13px, color:var(--ink-30), 
display as footnote with asterisk.

---

## PART 7 — STEP 1: KOSHER SELECTION

Three large cards in a row (or 2+1 on mobile):

🔴 בשרי  |  🔵 חלבי  |  🟢 פרווה

Each card has at the top a large colored circle (60px diameter) containing the emoji:
- בשרי: circle background var(--meat), emoji 🥩
- חלבי: circle background var(--milk), emoji 🧀  
- פרווה: circle background var(--pareve), emoji 🌿

Card content:
- Name in Playfair Display 22px bold
- Short description
- Kosher rule badge at bottom

When פרווה is selected, immediately show a smooth animated sub-panel below the cards:
```
"באילו כלים תרצו להשתמש?"
[🥩 כלים בשריים]  [🧀 כלים חלביים]
```
This pareve equipment choice (pareveEquipType) is passed to step 2.

---

## PART 8 — STEP 2: EQUIPMENT (STRICTLY KOSHER-ENFORCED)

CRITICAL KOSHER RULE — enforce in UI:
- kosherType==='meat' OR (kosherType==='pareve' AND pareveEquipType==='meat'):
  Show ONLY meat equipment. Never show dairy equipment.
- kosherType==='dairy' OR (kosherType==='pareve' AND pareveEquipType==='dairy'):
  Show ONLY dairy equipment. Never show meat equipment.

This is halachic law. The UI must make it impossible to select wrong equipment.

Equipment data (src/data/equipment.js):
```js
export const EQUIPMENT = {
  meat: [
    { id: 'oven_m',    label: 'תנור בשרי',         emoji: '🔥' },
    { id: 'pan_m',     label: 'מחבת בשרית',        emoji: '🍳' },
    { id: 'pot_m',     label: 'סיר בשרי',          emoji: '🫕' },
    { id: 'grill',     label: 'גריל/מנגל',         emoji: '🔥' },
    { id: 'press_m',   label: 'סיר לחץ בשרי',      emoji: '⚡' },
    { id: 'slow_m',    label: 'סלואו קוקר בשרי',   emoji: '🍲' },
    { id: 'griddle_m', label: 'מחבת פסים בשרית',   emoji: '🥩' },
  ],
  dairy: [
    { id: 'oven_d',    label: 'תנור חלבי',         emoji: '🔥' },
    { id: 'pan_d',     label: 'מחבת חלבית',        emoji: '🍳' },
    { id: 'pot_d',     label: 'סיר חלבי',          emoji: '🫕' },
    { id: 'mixer',     label: 'מיקסר',             emoji: '⚡' },
    { id: 'blender',   label: 'בלנדר',             emoji: '🫙' },
    { id: 'processor', label: 'מעבד מזון חלבי',    emoji: '⚙️' },
    { id: 'griddle_d', label: 'מחבת פסים חלבית',   emoji: '🧀' },
  ],
  general: [
    { id: 'wok',       label: 'וואק',              emoji: '🍳' },
    { id: 'airfryer',  label: 'אייר פריייר',       emoji: '💨' },
    { id: 'microwave', label: 'מיקרוגל',           emoji: '📡' },
    { id: 'steamer',   label: 'מאדה',              emoji: '♨️' },
    { id: 'toaster',   label: 'טוסטר אובן',        emoji: '🍞' },
  ]
};
```

Show equipment chips in a clean card layout. 
Saved equipment from Firestore auto-selected when user is logged in.

---

## PART 9 — STEP 3: CATEGORIES WITH IMAGES + COLORED CIRCLES

Each category card has:
1. Photo background (Unsplash URL)
2. Colored dot indicator top-right: red=meat, blue=dairy, green=pareve
3. Playfair Display name overlay
4. Short description

Categories data (src/data/categories.js):
```js
export const CATEGORIES = {
  meat: [
    { id: 'roast',   name: 'תנורי',       desc: 'כרעיים, כנפיים, תבשיל', emoji: '🍗',
      img: 'https://images.unsplash.com/photo-1598103442097-8b74394b95c8?w=400&q=75',
      dishes: ['כרעיים בתנור','כנפיים','חזה ממולא','עוף שלם','ירכיים'] },
    { id: 'pan',     name: 'מוקפץ',       desc: 'מחבת, סטייק, קציצות',   emoji: '🥩',
      img: 'https://images.unsplash.com/photo-1607116667981-ff148a4c6ea9?w=400&q=75',
      dishes: ['סטייק','קציצות','שניצל','חתיכות מוקפצות','מרגז'] },
    { id: 'stew',    name: 'תבשיל',       desc: 'מרק, חמין, תבשיל',       emoji: '🍲',
      img: 'https://images.unsplash.com/photo-1547592166-23ac45744acd?w=400&q=75',
      dishes: ['מרק עוף','תבשיל ירקות','חמין','עוף בירקות','כתף בתנור'] },
    { id: 'grill',   name: 'גריל',        desc: 'שיפודים, בשר, נקניקיות', emoji: '🔥',
      img: 'https://images.unsplash.com/photo-1555939594-58d7cb561ad1?w=400&q=75',
      dishes: ['שיפודים','אסאדו','נקניקיות','המבורגר','כנפיים גריל'] },
    { id: 'asian_m', name: 'אסייתי בשרי', desc: 'וואק, טריאקי, נודלס',   emoji: '🍜',
      img: 'https://images.unsplash.com/photo-1569050467447-ce54b3bbc37d?w=400&q=75',
      dishes: ['נודלס עוף','אורז מוקפץ','קארי עוף','טריאקי עוף','פאד תאי'] },
  ],
  dairy: [
    { id: 'pasta',   name: 'פסטה',        desc: 'ספגטי, פנה, ריגאטוני',  emoji: '🍝',
      img: 'https://images.unsplash.com/photo-1551183053-bf91798d13f4?w=400&q=75',
      dishes: ['ספגטי','פנה','ריגאטוני','לזניה','ניוקי','גנוצ\'י'] },
    { id: 'pie',     name: 'פשטידה / קיש',desc: 'קיש, פשטידה, מאפה',     emoji: '🥧',
      img: 'https://images.unsplash.com/photo-1571091718767-18b5b1457add?w=400&q=75',
      dishes: ['פשטידה','קיש','בורקס','סופלה','פריטטה'] },
    { id: 'eggs',    name: 'ביצים',       desc: 'שקשוקה, חביתה, אמלט',   emoji: '🍳',
      img: 'https://images.unsplash.com/photo-1533089860892-a7c6f0a88666?w=400&q=75',
      dishes: ['שקשוקה','חביתה','אמלט','ביצים מקושקשות','ביצים בעיניים'] },
    { id: 'asian_d', name: 'אסייתי חלבי', desc: 'נודלס, ריזוטו, קארי',   emoji: '🍜',
      img: 'https://images.unsplash.com/photo-1569050467447-ce54b3bbc37d?w=400&q=75',
      dishes: ['נודלס מוקפץ','ריזוטו','קארי ירקות','פאד תאי חלבי'] },
    { id: 'bake',    name: 'אפייה',       desc: 'עוגות, לחם, מאפינס',    emoji: '🥐',
      img: 'https://images.unsplash.com/photo-1555507036-ab1f4038808a?w=400&q=75',
      dishes: ['עוגת שוקולד','עוגיות','בננה בראד','לחם','מאפינס','בראוניז'] },
  ],
  pareve: [
    { id: 'fish',    name: 'דגים',        desc: 'סלמון, טונה, דג נילוס',  emoji: '🐟',
      img: 'https://images.unsplash.com/photo-1519708227418-c8fd9a32b7a2?w=400&q=75',
      dishes: ['סלמון בתנור','טונה מוקפצת','דג נילוס','סלמון מעושן','פילה בקלה'] },
    { id: 'salad',   name: 'סלטים',       desc: 'טרי, קל, ים-תיכוני',    emoji: '🥗',
      img: 'https://images.unsplash.com/photo-1512621776951-a57141f2eefd?w=400&q=75',
      dishes: ['סלט ים תיכוני','טאבולה','סלט קינואה','סלט עדשים','פלאפל'] },
    { id: 'soup_p',  name: 'מרק פרווה',   desc: 'עדשים, ירקות, קטניות',  emoji: '🫕',
      img: 'https://images.unsplash.com/photo-1547592166-23ac45744acd?w=400&q=75',
      dishes: ['מרק עדשים','מרק ירקות','מרק עגבניות','מרק דלעת','מינסטרונה'] },
    { id: 'legumes', name: 'קטניות',      desc: 'חומוס, שעועית, עדשים',  emoji: '🫘',
      img: 'https://images.unsplash.com/photo-1585032226651-759b368d7246?w=400&q=75',
      dishes: ['חומוס ביתי','פול מדמס','שעועית ברוטב','עדשים עם אורז','מג\'דרה'] },
  ]
};
```

---

## PART 10 — STEP 5: INGREDIENTS (RICH LISTS + "OTHER" FIELDS)

Data in src/data/ingredients.js and src/data/sauces.js.

### Proteins by kosher type:

MEAT proteins:
כרעיים, כנפיים, חזה עוף, עוף טחון, ירכיים, שניצל עוף, משולש, 
אנטריקוט, בשר בקר טחון, כתף בקר, כבש טחון, נקניקיות, שיפודי עוף, 
קציצות מוכנות, חזה הודו

DAIRY proteins:
ביצים, גבינה צהובה, גבינה לבנה 5%, גבינה לבנה 9%, ריקוטה, 
גבינה בולגרית, גבינת קוטג', שמנת חמוצה, חמאה, חלב, 
שמנת לבישול, יוגורט טבעי, גבינת מוצרלה, גבינת פרמזן, גבינת ברי

FISH/PAREVE proteins:
סלמון טרי, סלמון מעושן, טונה בשמן, טונה במים, דג נילוס, 
פילה בקלה, חזה הודו (פרווה), גרגרי חומוס, עדשים כתומות, 
עדשים שחורות, שעועית לבנה, שעועית אדומה, טופו, 
אטריות אורז, קינואה

### Sauces (src/data/sauces.js):
Filter automatically — remove dairy sauces when meat, remove meat sauces when dairy.

ALL_SAUCES array with kosherFlags:
```js
export const SAUCES = [
  { label: 'רסק עגבניות',    emoji: '🍅', kosher: 'all'   },
  { label: 'שמן זית',        emoji: '🫒', kosher: 'all'   },
  { label: 'סויה',           emoji: '🫙', kosher: 'all'   },
  { label: 'טריאקי',         emoji: '🫙', kosher: 'all'   },
  { label: 'דבש',            emoji: '🍯', kosher: 'all'   },
  { label: 'טחינה גולמית',   emoji: '🫙', kosher: 'all'   },
  { label: 'טחינה מוכנה',    emoji: '🫙', kosher: 'all'   },
  { label: 'חריסה',          emoji: '🌶️', kosher: 'all'   },
  { label: 'חרדל דיז\'ון',   emoji: '🫙', kosher: 'all'   },
  { label: 'חרדל רגיל',      emoji: '🫙', kosher: 'all'   },
  { label: 'BBQ',            emoji: '🫙', kosher: 'meat'  },
  { label: 'פסטו',           emoji: '🌿', kosher: 'dairy' },
  { label: 'שמנת לבישול',    emoji: '🫙', kosher: 'dairy' },
  { label: 'צ\'ילי מתוק',    emoji: '🌶️', kosher: 'all'   },
  { label: 'חלב קוקוס',      emoji: '🥥', kosher: 'all'   },
  { label: 'שמן שומשום',     emoji: '🫙', kosher: 'all'   },
  { label: 'בלסמי',          emoji: '🫙', kosher: 'all'   },
  { label: 'ג\'ינג\'ר טרי',  emoji: '🫚', kosher: 'all'   },
  { label: 'מייפל',          emoji: '🍁', kosher: 'all'   },
  { label: 'ווסטרשייר',      emoji: '🫙', kosher: 'meat'  },
  { label: 'טבסקו',          emoji: '🌶️', kosher: 'all'   },
  { label: 'שמנת חמוצה',     emoji: '🫙', kosher: 'dairy' },
  { label: 'לימון סחוט',     emoji: '🍋', kosher: 'all'   },
  { label: 'תמריד',          emoji: '🫙', kosher: 'all'   },
  { label: 'מיסו',           emoji: '🫙', kosher: 'all'   },
  { label: 'סריראצ\'ה',      emoji: '🌶️', kosher: 'all'   },
  { label: 'שמן קוקוס',      emoji: '🥥', kosher: 'all'   },
  { label: 'רוטב צדף',       emoji: '🫙', kosher: 'pareve'},
  { label: 'עלי נענע טרי',   emoji: '🌿', kosher: 'all'   },
  { label: 'יין לבן לבישול', emoji: '🍷', kosher: 'all'   },
];
```

### Vegetables (18+ items):
בצל, שום, עגבנייה, תפוח אדמה, גזר, פלפל אדום, פלפל צהוב, פלפל ירוק,
קישוא, חציל, ברוקולי, תרד, פטריות שמפיניון, פטריות פורטובלו, 
אבוקדו, עגבניות שרי, כרוב לבן, כרוב סגול, לימון, בטטה, 
סלרי, כרישה, אפונה קפואה, תירס קפוא, עגבניות מרוסקות בקופסה, שעועית ירוקה

### Spices (14+ items):
מלח ופלפל שחור, כמון, פפריקה מתוקה, פפריקה חריפה, כורכום,
קינמון, אורגנו, בזיליקום יבש, צ'ילי פתיתים, זעתר, בהרט,
רוזמרין, תימין, כוסברה יבשה, פלפל לבן, הל, זנגביל יבש

### "Other" free-text fields — add after EACH ingredient section:
```jsx
<div className="other-field">
  <span className="other-label">+ הוסיפו מה שחסר:</span>
  <input 
    type="text" 
    placeholder="למשל: שעועית ירוקה, ענבים, גבינת עיזים..."
    value={extrasForSection}
    onChange={...}
  />
</div>
```

---

## PART 11 — STEP 5 ADDITIONAL OPTIONS

Add these at the bottom of step 5, before servings:

### Recipe idea field:
```jsx
<div className="card optional-card">
  <label className="card-title">💡 יש לכם רעיון? (אופציונלי)</label>
  <textarea
    placeholder="למשל: 'משהו בסגנון אסייתי עם אטריות' או 'עוגה שמתאימה לשבת'..."
    rows={3}
    value={recipeIdea}
    onChange={...}
    maxLength={200}
  />
</div>
```

### Difficulty selector:
```jsx
<div className="card">
  <div className="card-title">⭐ דרגת קושי מועדפת</div>
  <div className="difficulty-row">
    {['easy','medium','hard'].map(d => (
      <div 
        className={`difficulty-chip ${difficulty===d?'on':''}`}
        onClick={() => setDifficulty(d)}
      >
        {d==='easy' ? '😌 קל' : d==='medium' ? '🙂 בינוני' : '💪 מאתגר'}
      </div>
    ))}
  </div>
</div>
```

### Max time field:
```jsx
<div className="card optional-card">
  <div className="card-title">⏱️ זמן מקסימלי (אופציונלי)</div>
  <div className="time-row">
    <input 
      type="number" 
      min="10" max="240" step="5"
      placeholder="—"
      value={maxMinutes}
      onChange={...}
    />
    <span>דקות</span>
  </div>
  <div className="card-sub">השאירו ריק אם אין מגבלת זמן</div>
</div>
```

---

## PART 12 — OPENAI PROMPT (ROBUST + KOSHER-SAFE)

In netlify/functions/recipe.js, use this exact prompt builder:
```js
exports.handler = async (event) => {
  if (event.httpMethod !== 'POST') return { statusCode: 405, body: 'Method Not Allowed' };
  
  const apiKey = process.env.OPENAI_API_KEY;
  if (!apiKey) return { statusCode: 500, body: JSON.stringify({ error: 'API key missing' }) };

  const body = JSON.parse(event.body);
  const {
    category, kosherType, equipmentType,
    dishType, proteins, sauces, vegetables,
    spices, extrasProteins, extrasSauces, 
    extrasVegetables, extrasSpices,
    equipment, servings, recipeIdea, 
    difficulty, maxMinutes
  } = body;

  // Build extras safely — sanitize free text
  const sanitize = (str) => str ? str.replace(/[<>{}]/g, '').slice(0, 100) : '';
  const safeIdea = sanitize(recipeIdea);

  const allIngredients = [
    ...(proteins || []),
    ...(sanitize(extrasProteins).split(',').map(s=>s.trim()).filter(Boolean)),
    ...(sauces || []),
    ...(sanitize(extrasSauces).split(',').map(s=>s.trim()).filter(Boolean)),
    ...(vegetables || []),
    ...(sanitize(extrasVegetables).split(',').map(s=>s.trim()).filter(Boolean)),
    ...(spices || []),
    ...(sanitize(extrasSpices).split(',').map(s=>s.trim()).filter(Boolean)),
  ].filter(Boolean);

  const kosherRule = kosherType === 'meat'
    ? `⚠️ ABSOLUTE KOSHER LAW — MEAT (בשרי):
       This recipe is 100% meat-based.
       STRICTLY FORBIDDEN: ANY dairy product — cheese, butter, milk, cream, 
       yogurt, sour cream, whey, lactose. NO exceptions. NO substitutions.
       This is an inviolable halachic requirement.`
    : kosherType === 'dairy'
    ? `⚠️ ABSOLUTE KOSHER LAW — DAIRY (חלבי):
       This recipe is 100% dairy-based.
       STRICTLY FORBIDDEN: ANY meat product — chicken, beef, lamb, veal,
       meat stock, gelatin from meat. NO exceptions.
       This is an inviolable halachic requirement.`
    : `⚠️ KOSHER LAW — PAREVE (פרווה):
       No meat AND no dairy ingredients.
       Equipment used: ${equipmentType === 'meat' ? 'meat utensils' : 'dairy utensils'} — 
       this affects which ingredients are appropriate.`;

  const difficultyGuide = difficulty === 'easy'
    ? 'DIFFICULTY: Easy — max 30 minutes total, minimal equipment, beginner-friendly. Simple techniques only.'
    : difficulty === 'hard'
    ? 'DIFFICULTY: Advanced — complex techniques allowed, no time restriction.'
    : 'DIFFICULTY: Medium — up to 1 hour, standard home cooking techniques.';

  const timeConstraint = maxMinutes
    ? `⏱️ HARD TIME LIMIT: Total cooking + prep time MUST NOT exceed ${maxMinutes} minutes. Design the recipe accordingly.`
    : '';

  const ideaGuide = safeIdea
    ? `USER SUGGESTION: "${safeIdea}"
       Instructions: Use this as creative inspiration ONLY IF it:
       (a) complies with the kosher rules above
       (b) makes culinary sense with the available ingredients
       (c) is a reasonable, real food concept
       If the suggestion is nonsensical, impossible, offensive, or non-kosher — 
       SILENTLY IGNORE IT and create the best recipe from available ingredients.
       Never mention that you ignored or modified the suggestion.`
    : '';

  const prompt = `You are a master Israeli kosher chef with 30 years of experience 
in traditional and modern kosher cuisine.

YOUR TASK: Create exactly ONE complete, delicious, realistic recipe in Hebrew.

${kosherRule}

RECIPE PARAMETERS:
- Category: ${category}
${dishType ? `- Specific dish: ${dishType}` : ''}  
- Servings: ${servings}
- ${difficultyGuide}
${timeConstraint}
${ideaGuide}

AVAILABLE INGREDIENTS:
${allIngredients.length > 0 ? allIngredients.join(', ') : '(use basic pantry items)'}

AVAILABLE EQUIPMENT: ${(equipment || []).join(', ') || 'standard kitchen equipment'}

CHEF RULES:
1. Use ONLY the listed ingredients (salt, black pepper, oil, water always allowed)
2. Do not invent unavailable ingredients
3. Recipe MUST work with the listed equipment
4. Quantities must be precise and correctly scaled for ${servings} servings
5. Steps must be clear enough for a home cook to follow
6. The title must be creative and appetizing in Hebrew
7. If user-provided extras seem like nonsense, invent plausible ingredients that fit

RESPONSE FORMAT — follow exactly, no deviations:

TITLE: [creative appetizing Hebrew name]
TIME_PREP: [X דקות]
TIME_COOK: [Y דקות]  
DIFFICULTY: [קל / בינוני / מאתגר]

INGREDIENTS:
- [ingredient — exact quantity for ${servings} servings]
- [continue...]

STEPS:
1. [detailed step]
2. [detailed step]
(minimum 5 steps, maximum 10)

TIP: [one professional tip that genuinely elevates the dish]`;

  try {
    const response = await fetch('https://api.openai.com/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${apiKey}`
      },
      body: JSON.stringify({
        model: 'gpt-4o',
        messages: [{ role: 'user', content: prompt }],
        max_tokens: 1400,
        temperature: 0.75
      })
    });

    if (!response.ok) {
      const err = await response.text();
      return { statusCode: response.status, body: JSON.stringify({ error: err }) };
    }

    const data = await response.json();
    const text = data.choices?.[0]?.message?.content || '';
    return {
      statusCode: 200,
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ recipe: text })
    };
  } catch (err) {
    return { statusCode: 500, body: JSON.stringify({ error: err.message }) };
  }
};
```

---

## PART 13 — FIREBASE (src/firebase.js)
```js
import { initializeApp } from 'firebase/app';
import { getAuth, GoogleAuthProvider, OAuthProvider, 
         signInWithPopup, signInAnonymously, signOut } from 'firebase/auth';
import { getFirestore, doc, getDoc, setDoc, 
         collection, query, orderBy, limit, 
         getDocs, addDoc, serverTimestamp } from 'firebase/firestore';

const firebaseConfig = {
  apiKey: "AIzaSyABfmxXuZlzHIXuGx0cHKxAdctQ_ep-_sA",
  authDomain: "mayeshrecipes.firebaseapp.com",
  projectId: "mayeshrecipes",
  storageBucket: "mayeshrecipes.firebasestorage.app",
  messagingSenderId: "261954124",
  appId: "1:261954124:web:decb95f2d5b2b2e72cb2e9"
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const db = getFirestore(app);
export const googleProvider = new GoogleAuthProvider();
export const microsoftProvider = new OAuthProvider('microsoft.com');

// Auth helpers
export const signInGoogle    = () => signInWithPopup(auth, googleProvider);
export const signInMicrosoft = () => signInWithPopup(auth, microsoftProvider);
export const signInGuest     = () => signInAnonymously(auth);
export const doSignOut       = () => signOut(auth);

// Firestore helpers
export const getUserDoc  = (uid) => getDoc(doc(db, 'users', uid));
export const saveUserDoc = (uid, data) => setDoc(doc(db, 'users', uid), data, { merge: true });

export const saveRecipe = (uid, recipeData) =>
  addDoc(collection(db, 'users', uid, 'recipes'), {
    ...recipeData, createdAt: serverTimestamp()
  });

export const getRecentRecipes = async (uid) => {
  const q = query(
    collection(db, 'users', uid, 'recipes'),
    orderBy('createdAt', 'desc'),
    limit(5)
  );
  const snap = await getDocs(q);
  return snap.docs.map(d => ({ id: d.id, ...d.data() }));
};
```

### Firestore data structure:
```
users/{uid}
  ├── gender: 'male' | 'female'
  ├── equipment: string[]
  ├── equipmentType: 'meat' | 'dairy'
  └── recipes/{id}
        ├── title: string
        ├── category: string
        ├── kosher: 'meat' | 'dairy' | 'pareve'
        ├── servings: number
        ├── difficulty: string
        └── createdAt: timestamp
```

---

## PART 14 — NETLIFY.TOML
```toml
[build]
  base    = "recipe-app"
  publish = "dist"
  command = "npm run build"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
```

---

## PART 15 — PROFILE PANEL (components/ProfilePanel.jsx)

Slide-in panel from the left side, appears when clicking avatar in AuthBar.
Close on outside click.

Contents:
1. User avatar + name + email (or "אורח/ת" for anonymous)
2. Section: "מתכונים אחרונים" — 5 recent recipes as clickable cards
   - Each shows: title, category emoji, kosher badge, date
3. Section: "הציוד שלי" — equipment chips (same as step 2)
   - Auto-loaded from Firestore
   - "שמור ציוד" button → saves to Firestore + updates step 2
4. Sign out button (hidden for anonymous users)

---

## FINAL INSTRUCTIONS FOR CLAUDE CODE:

1. Create ALL files in the structure above — no placeholders, fully functional code
2. Run `npm install` after creating package.json
3. Verify `npm run dev` starts without errors
4. Ensure the Vite proxy correctly forwards /api/recipe to localhost:9999
5. The local function server must read OPENAI_API_KEY from .env
6. Create a .env file with placeholder: OPENAI_API_KEY=your-key-here
7. All text direction: RTL (dir="rtl" on html element)
8. The gender selection screen must appear once per user and never again
9. Kosher enforcement in equipment is mandatory — wrong tools must never appear
10. After finishing, print:
    "✅ Done! To run locally:
     1. Edit .env and add your OPENAI_API_KEY
     2. Run: npm run dev
     3. Open: http://localhost:5173"